import React from 'react';
import Scheduler from "../components/Scheduler";

const Appointment = () => {
    return (
        <div>
            <Scheduler />
        </div>
    );
};

export default Appointment;